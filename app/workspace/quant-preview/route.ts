import {NextResponse} from "next/server";import {createQuantTask} from "@/app/lib/quant-research";
export async function POST(request:Request){try{const body=await request.json() as {goal?:string};const task=createQuantTask(body.goal??"");return NextResponse.json({type:"quant_workspace_preview",task_id:task.id,extraction:task.extraction,patch:task.workspacePatch,requires_confirmation:true});}catch(error){return NextResponse.json({message:error instanceof Error?error.message:"无法生成预览"},{status:422});}}
